apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filebrowser-pvc
  namespace: filebrowser
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: smb-filebrowser
  resources:
    requests:
      storage: 10Gi